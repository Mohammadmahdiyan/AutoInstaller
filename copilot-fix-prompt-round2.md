# پرامپت دور دوم برای GitHub Copilot (Agent/Chat mode در VS Code)

پروژه GtaSaModManager (WinForms، .NET 8). یه دور فیکس قبلاً انجام شده ولی چندتا مورد
یا اصلاً فیکس نشدن یا نصفه‌کاره فیکس شدن. لطفاً هر مورد رو دقیق و کامل انجام بده،
نه فقط یه patch سطحی — قبل از هرکدوم، کد مرتبط رو کامل بخون و ریشه‌ی واقعی مشکل رو
پیدا کن.

⚠️ موارد ۸ و ۹ (columns و category در Step 5) قبلاً حداقل دوبار سعی شده فیکس بشن ولی
هنوز کار نمی‌کنن. لطفاً این‌بار به‌جای اصلاح جزئی، کل مسیر داده (از انتخاب combo تا
رندر شدن گرید) رو ردیابی کن و مطمئن شو واقعاً اثر داره — بعد از فیکس، خودت شبیه‌سازی
کن (یا حداقل با منطق دقیق چک کن) که مقدار انتخابی combo واقعاً روی خروجی اثر می‌ذاره.

---

## ۱. دکمه‌های Browse (RoundedButton) هنوز درست نیست
فایل: UI/RoundedButton.cs

**۱.۱ هاور نرم (انیمیشن) نداره**
الان `OnMouseEnter`/`OnMouseLeave` فقط `Invalidate()` صدا می‌زنن که یه پرش ناگهانی
رنگه، نه ترنزیشن نرم. یه `System.Windows.Forms.Timer` (یا `System.Windows.Forms.Timer`
با interval ~15ms) اضافه کن که یه مقدار `_hoverProgress` (float بین 0 و 1) رو در طول
~150-200ms به‌سمت 0 یا 1 (بسته به هاور بودن) نرم حرکت بده، و در OnPaint رنگ fillColor
رو با `ColorLerp(normalColor, hoverColor, _hoverProgress)` محاسبه کن، نه یه switch دوحالته.

**۱.۲ رنگ آبی به‌شکل حلقه/رینگ دیده میشه، نه یکدست**
علتش اینه که `this.Region` هر بار در OnPaint با `Rectangle(1,1,Width-2,Height-2)`
محاسبه میشه، ولی گرادیانت/بردر با ضخامت pen جدا رسم میشه و لبه‌های anti-alias شده
با پس‌زمینه‌ی parent (که رنگ فرق داره) قاطی میشن. لطفاً:
- `Region` رو فقط یک‌بار (در `OnResize` یا وقتی سایز عوض میشه) محاسبه و ست کن، نه هر
  فریم OnPaint.
- مطمئن شو parent panel که این دکمه روش قرار می‌گیره، `BackColor` دقیقاً با پس‌زمینه‌ی
  اطراف یکی باشه، یا از `SetStyle(ControlStyles.SupportsTransparentBackColor, true)`
  + `BackColor = Color.Transparent` استفاده کن تا لبه‌ها با پس‌زمینه‌ی واقعی ترکیب بشن نه رنگ ثابت.
- به‌جای اینکه کل داخل دکمه رو موقع هاور یکدست آبی روشن کنی، فقط با یه گرادیانت ملایم‌تر
  (تغییر ۸-۱۰٪ روشنایی نسبت به رنگ پایه، نه یه آبی کاملاً متفاوت مثل `59,130,246`) رنگ
  رو عوض کن تا حس یکدست بودن حفظ بشه.

**۱.۳ رنگ باید از تم بیاد، نه هاردکد**
الان `BackColor = Color.FromArgb(37, 99, 235)` توی سازنده هاردکده. لطفاً یه پراپرتی
`public Color AccentColor { get; set; }` اضافه کن که پیش‌فرضش از `ThemeManager` (رنگ
اصلی/accent تم فعلی) خونده بشه، و در OnPaint به‌جای `BackColor` ثابت، از `AccentColor`
استفاده کن. وقتی تم عوض میشه (دارک/لایت)، `ThemeManager` باید `AccentColor` همه‌ی
`RoundedButton`های صفحه رو آپدیت کنه و `Invalidate()` کنه.

---

## ۲. لایوت سایدبار Step 4 (عکس + readme) هنوز پیاده نشده
فایل: Forms/MainForm.cs، متد UpdateSidebarState (~خط 3550 به بعد)
با اینکه قبلاً خواسته شده بود، عکس‌ها فرقی نکردن و README اصلاً نشون داده نمیشه.
این‌بار دقیق‌تر توضیح میدم، لطفاً از صفر با TableLayoutPanel بساز (نه TextBox/PictureBox
ثابت فعلی):

**اگه README باشه:**
- یه باکس متنی read-only که متن با **word-wrap** نشون داده بشه (مثل حالت Alt+Z توی
  VS Code — یعنی خطای طولانی می‌شکنه به خط بعد، نه اسکرول افقی).
- زیرِ این باکس، یه دکمه «Open readme.txt» که فایل رو با برنامه‌ی پیش‌فرض سیستم باز کنه
  (`Process.Start` با `UseShellExecute = true`).
- اگه README نبود، این کل بخش (باکس متن + دکمه) اصلاً رندر نشه، جاش خالی نمونه (فضاش
  به بخش عکس بره).

**فضای باقی‌مونده (از پایین باکس README تا انتهای پنجره، یا از زیر Input تا انتهای
پنجره اگه README نبود) برای عکس‌هاست:**
- ۰ عکس → هیچی نشون داده نشه.
- ۱ عکس → کل همون فضا (به‌اندازه‌ی جای ۴ تا عکس) به همین یکی اختصاص پیدا کنه.
- ۲ عکس → هر دو بالای هم/کنار هم به‌طور مساوی.
- ۳ عکس → دوتای اول عادی، سومی طبق جهت RTL/LTR (تنظیمات زبان فعلی) سمت راست یا چپ چیده بشه.
- ۴ عکس → هر ۴ تا بدون دکمه.
- بیشتر از ۴ عکس → همون ۴ تای اول با دو دکمه Next/Prev (آیکون) برای رفتن به عکس بعدی/قبلی.
- تمام پدینگ‌های استاندارد UI پروژه (که جاهای دیگه استفاده شده) رعایت بشه.

---

## ۳. راست‌کلیک روی عکس‌ها در Step 4/5/6 پنجره‌ی تمام‌صفحه باز نمی‌کنه
فایل: Forms/MainForm.cs
تابع `OpenImageViewerFromPictureBox` از قبل نوشته شده ولی ظاهراً به همه‌ی PictureBoxهای
Step4/5/6 وصل نیست. لطفاً:
1. همه‌ی جاهایی که PictureBox برای نمایش عکس مود/asset ساخته میشه (گالری Step4، گالری
   Step5 (کارت‌های asset)، گالری Step6، عکس سایدبار) رو پیدا کن.
2. برای هرکدوم `pictureBox.MouseUp += (_, e) => { if (e.Button == MouseButtons.Right) OpenImageViewerFromPictureBox(pictureBox, imageListForThisGallery); };` رو وصل کن (اگه هنوز وصل نیست).
3. تست کن که `imageListForThisGallery` درست پاس داده میشه (نه لیست خالی یا لیست اشتباه).

---

## ۴. متن «Mod detected» باید بره توی سایدبار
الان جای دیگه‌ایه (احتمالاً بالای گالری Step4). لطفاً این لیبل رو به سایدبار (بالای
بخش README/عکس که در بند ۲ توضیح دادم) منتقل کن.

---

## ۵. لودینگ بین Step4 و Step5 واقعی نیست + دو تا لودینگ دیگه لازمه

**۵.۱ لودینگ فعلی خیلی کوتاهه و blur نداره**
فایل: Forms/MainForm.cs، `ShowStep4LoadingTransitionAsync`
```csharp
private async Task ShowStep4LoadingTransitionAsync(string? assetType)
{
    ShowStep4LoadingOverlay(assetType);
    await Task.Delay(350);
    HideStep4LoadingOverlay();
}
```
این فقط ۳۵۰ میلی‌ثانیه صبر می‌کنه و ربطی به زمان واقعی لود شدن آیتم‌ها نداره — برای
همینه که کاربر یه فلاش سفید کوتاه می‌بینه و بعد چند ثانیه دیگه گیر می‌کنه چون گرید
هنوز واقعاً لود نشده. لطفاً:
1. Overlay رو **قبل از شروع** پرکردن گرید Step5 نشون بده (نه با تایمر ثابت).
2. پرکردن گرید (`RefreshAssetStep` و ساخت PictureBoxها/بارگذاری تصاویر thumbnail) رو
   به‌صورت async انجام بده (I/O سنگین مثل خوندن فایل تصویر رو با `Task.Run` ببر رو
   thread پس‌زمینه، فقط ساخت کنترل‌های UI روی UI thread بمونه).
3. بعد از اینکه واقعاً همه‌ی کارت‌ها ساخته و آماده‌ی نمایش شدن، Overlay رو مخفی کن —
   نه با یه Delay ثابت و حدسی.
4. روی خودِ Overlay، پس‌زمینه‌ش رو به‌جای رنگ ثابت (`245,247,250`)، یه افکت **blur** روی
   محتوای پشتش بذار (چون WinForms خودش blur نداره، ساده‌ترین راه: یه اسکرین‌شات از
   پنل زیرین بگیر، با `System.Drawing` یه Gaussian blur ساده (یا فقط downscale+upscale
   برای افکت بلور ارزون) روش اعمال کن، و همون تصویر بلورشده رو به‌عنوان BackgroundImage
   ست کن)، به‌علاوه یه spinner واقعی که می‌چرخه (نه ایموجی ثابت ⏳ — یا با یه Timer
   زاویه‌ی یه آیکون/دایره رو بچرخون، یا از یه GIF/انیمیشن ساده استفاده کن).

**۵.۲ موقع تغییر زبان هم باید همین افکت blur+loading باشه**
فایل: Forms/MainForm.cs، هندلر تغییر زبان
همون overlay بلور (یا نسخه‌ی سراسری‌ش که از قبل با نام `_globalLoadingOverlay` ساخته
شده) رو قبل از `RefreshModList()`/`RefreshModLibrary()`/`ApplyLocalization()` نشون بده،
و بعد از اتمام کامل همه‌ی این عملیات‌ها مخفی کن.

**۵.۳ انتخاب آیتم در Step 5 هم یه لحظه هنگ می‌کنه**
وقتی کاربر یه asset رو در گرید Step5 انتخاب می‌کنه (کلیک روی کارت)، اگه این باعث
یه عملیات سنگین (مثل لود دوباره‌ی تصویر سایدبار، یا محاسبه‌ی مجدد چیزی) میشه، همون
overlay (نسخه‌ی سبک‌تر، بدون blur لازم نیست، فقط یه spinner کوچیک گوشه‌ی گرید) رو
نشون بده تا عملیات تموم بشه.

---

## ۶. رنگ کارت‌های Step5 باید از تم بیاد
الان پس‌زمینه‌ی کارت‌های asset (پشت عکس‌ها) و رنگ متن‌شون مستقل از تمه. لطفاً این
مقادیر رو از همون سیستم رنگی که `ThemeManager` برای بقیه‌ی اجزا استفاده می‌کنه
بخون (نه رنگ هاردکد)، طوری که وقتی کاربر بین تم دارک/لایت سوییچ می‌کنه، این کارت‌ها
هم فوراً آپدیت بشن.

---

## ۷. بعد از تغییر زبان، بعضی متن‌ها همچنان انگلیسیه
فایل: Services/LocalizationService.cs + هرجا این متن‌ها هاردکد شدن
حداقل این دوتا مورد:
- "Image not available"
- "If you wish to select the model to be replaced..." (Step 5)
لطفاً `Forms/MainForm.cs` رو کامل grep کن دنبال هر رشته‌ی انگلیسی هاردکد (نه از طریق
`_localizationService.GetString(...)`)، و همه‌شون رو:
1. به کلید جدید تبدیل کن (مثلاً `ImageNotAvailable`, `Step5ReplaceModelHint`).
2. مقدار انگلیسی و فارسی هردو رو به دیکشنری‌های EnglishStrings و PersianStrings در
   LocalizationService اضافه کن.
3. مطمئن شو این متن‌ها بعد از عوض کردن زبان، در `ApplyCurrentLanguage`/`ApplyLocalization`
   واقعاً دوباره ست میشن (چون تا الان با اینکه ترجمه‌شون هست هم عوض نمیشدن، یعنی جایی
   که این کنترل‌ها ساخته میشن، رفرنس‌شون توی لیست آپدیت زبان نیست — این رفرنس‌ها رو پیدا
   کن و اضافه کن).

---

## ۸. Columns در Step 4/5 هنوز کار نمی‌کنه (فیکس قبلی جواب نداد)
فایل: Forms/MainForm.cs، `GetStep5CardSize` و `RefreshAssetStep`
لطفاً این‌بار:
1. چک کن `columns.SelectedIndexChanged` واقعاً فایر میشه و `_step5ColumnCount` مقدار
   جدید رو می‌گیره (یه `Debug.WriteLine` موقت بذار و مطمئن شو).
2. چک کن `GetStep5CardSize` واقعاً از `_step5ColumnCount` به‌درستی برای هر سه نوع
   Vehicle/Weapon/Skin استفاده می‌کنه، بدون هیچ `Math.Min`/`Math.Max` که مقدار رو به
   یه بازه‌ی باریک ثابت قفل کنه (این دقیقاً همون مشکلی بود که دفعه‌ی قبل گزارش شده بود
   — اگه هنوز چنین clampای هست، پاکش کن یا بازه‌ش رو خیلی بزرگ‌تر کن).
3. چک کن بعد از تغییر columns، گرید واقعاً دوباره rebuild/relayout میشه (نه فقط یه
   متغیر عوض بشه بدون اینکه `RefreshAssetStep` صدا زده بشه، یا `RefreshAssetStep` صدا
   زده بشه ولی کنترل‌های قبلی پاک نشن و کارت‌های قدیمی با سایز قدیم باقی بمونن).

## ۹. Category در Step 5 هنوز کار نمی‌کنه (فیکس قبلی جواب نداد)
فایل: Forms/MainForm.cs، `RefreshAssetStep` (~خط 1670، متغیر `hasMeaningfulCategories`)
لطفاً:
1. چک کن `hasMeaningfulCategories` واقعاً true میشه وقتی asset‌ها Category متنوع دارن
   (پرینت/دیباگ بگیر از تعداد category یکتا).
2. چک کن وقتی `categoryFilter.Visible = true` میشه، آیتم‌های combo واقعاً پر میشن
   (`categoryFilter.Items`) و شامل مقادیر واقعی category هستن، نه فقط "All".
3. چک کن انتخاب یه category واقعاً لیست asset نمایش‌داده‌شده رو فیلتر می‌کنه (خط فیلتر
   کردن `assets.Where(...)` بر اساس `_step5CategoryFilter` رو پیدا کن و مطمئن شو
   مقایسه‌ش درسته — مثلاً حساس به بزرگی/کوچکی حروف نباشه، فاصله‌ی اضافه نداشته باشه).

## ۱۰. آیدی هر آیتم در Step 5 نشون داده نمیشه
هر کارت asset باید علاوه بر اسم فایل، ID اون asset (از دیتای catalog/Assets.json) رو
هم به‌شکل کوچیک (مثلاً زیر اسم، یا موقع هاور تولتیپ) نشون بده.

## ۱۱. یه Select برای مرتب‌سازی در Step 5 اضافه کن
یه ComboBox کنار Columns/Category بذار با گزینه‌های «مرتب‌سازی بر اساس نام فایل» و
«مرتب‌سازی بر اساس ID»، که با تغییرش، لیست asset‌های نمایش داده‌شده در `RefreshAssetStep`
دوباره sort بشن.

## ۱۲. بازطراحی کامل بخش «مدل انتخابی» در سایدبار Step 5
الان فقط عکس/اسم/آیدی مدل انتخاب‌شده نشون داده میشه. باید این‌طوری بشه:
- **بالای عکسِ مدل مبدأ (اگه عکس مود payload وجود داشت؛ اگه نبود این قسمت اصلاً نشون داده نشه):**
  عکس فایل مبدأیی که کاربر می‌خواد جایگزین کنه رو بذار.
- از این عکس یه **فلش** به‌سمت عکس asset انتخاب‌شده بکش (یعنی: «این با این عوض میشه»)،
  و روی عکس asset انتخاب‌شده، اسم مود (اسم پوشه) رو به‌عنوان لیبل بنویس.
- عکس/آیدی/اسم فایل asset انتخاب‌شده رو مثل الان نشون بده، ولی موقع هاور روی عکس، اسمش
  رو هم (تولتیپ یا لیبل موقت) نشون بده.
- هر بار کاربر یه آیتم دیگه از گرید انتخاب می‌کنه، این بخش سایدبار باید **فوری** (بدون
  تاخیر محسوس) آپدیت بشه.
- **زیر این عکس، فضای باقی‌مونده باید به README اختصاص پیدا کنه** (همون منطق بند ۲،
  با یه دکمه‌ی «Open readme.txt» زیرش).

## ۱۳. بعضی آیتم‌های گرید Step 5 اصلاً دیده نمیشن (کات میشن) با اینکه اسکرول جا داره
گزارش دقیق کاربر: بعد از دو آیتم خاص (تو تست خودش با فایل‌هایی که اسمشون wmylg و
wmykara شروع میشه)، آیتم بعدی به‌جای نمایش کامل، فقط ~۳۰٪ از ارتفاعش دیده میشه و انگار
می‌ره «زیر» دو آیتم قبلی، در حالی که اسکرول هنوز جا داره (یعنی صفحه اسکرول میشه ولی
چیز جدیدی نشون داده نمیشه). این نشونه‌ی یه باگ در محاسبه‌ی موقعیت/ارتفاع ردیف‌ها یا
z-order کنترل‌هاست (احتمالاً TableLayoutPanel/FlowLayoutPanel که ارتفاع سطرها اشتباه
محاسبه شده، یا کارت‌ها به‌جای اضافه‌شدن به لایوت، مستقیم با مختصات دستی (`Location`)
جابه‌جا میشن و محاسبه‌ی مختصات ردیف بعدی اشتباهه). لطفاً کد ساخت گرید Step5 رو کامل
بازبینی کن؛ اگه از مختصات دستی (Location/Top/Left) استفاده شده، به یه Layout واقعی
(FlowLayoutPanel با WrapContents=true، یا TableLayoutPanel با تعداد ستون داینامیک) تغییرش
بده که خودش مسئولیت چیدمان درست رو به عهده بگیره و اسکرول‌پذیری واقعی داشته باشه.

## ۱۴. اسکرول‌بار در تم دارک زشته (سفید می‌مونه)
چون WinForms اسکرول‌بار native سیستم‌عاملو نشون میده که با تم برنامه هماهنگ نیست،
لطفاً یه custom scrollbar (یا با override کردن رفتار Panel و کشیدن یه نوار اسکرول
سفارشی با Paint، یا با یه پکیج/کنترل سبک WinForms-friendly) بساز که رنگش از
ThemeManager بیاد و برای هر دو تم دارک/لایت درست دیده بشه.

## ۱۵. در Step 5 بعد از انتخاب و زدن Install به Step 6 نمی‌ره
فایل: Forms/MainForm.cs، `HandleSidebarNext` (case WizardStep.Step5) و `InstallSelectedModAsync`
از نظر کد فعلی، این مسیر باید کار کنه: `_selectedAssetForInstall != null` → `InstallSelectedModAsync()`
→ چون `IsSingleAssetPackage`/`IsMultiAssetPackage` و `_selectedAssetForInstall != null`
هست → نصب → `GoToStep(WizardStep.Step6)`. چون کاربر میگه هنوز کار نمی‌کنه، لطفاً:
1. چک کن آیا `_selectedAssetForInstall` واقعاً درست موقع کلیک Install مقدار داره —
   مخصوصاً بعد از اینکه کاربر Columns یا Category رو عوض کرده (شاید `RefreshAssetStep`
   یا یه‌جای دیگه این مقدار رو بی‌خبر null می‌کنه).
2. یه try/catch دور کل بدنه‌ی `InstallSelectedModAsync` بذار (اگه نیست) که اگه Exception
   افتاد، به‌جای قورت دادن بی‌صدا، یه MessageBox با پیام خطای واقعی (`ex.Message` +
   `ex.StackTrace` در یه لاگ فایل برای دیباگ) نشون بده — این کمک می‌کنه اگه مشکل هنوز
   موند، بفهمیم دقیقاً کجا crash می‌کنه.
3. چک کن `InstallTypedPackageAsync` واقعاً return می‌کنه و throw نمی‌کنه وسط راه.

## ۱۶. Category برای ماشین‌ها در Step 5 نیست
(این تکرار/تکمیل باگ ۹ هست — مطمئن شو بعد از فیکس ۹، برای AssetType == "Vehicle" هم
category قابل تغییر باشه، نه فقط پیش‌فرض تشخیص داده‌شده.)

## ۱۸. پشتیبانی از فرمت Markdown برای فایل readme
فایل: Forms/MainForm.cs، `FindReadmeFile`
الان احتمالاً فقط `.txt` رو پیدا می‌کنه. لطفاً `readme.md`/`README.md` رو هم به لیست
اسم‌های قابل‌قبول اضافه کن، و اگه فایل `.md` بود، حداقل markdown ساده (headers با `#`،
bold با `**`، لیست با `-`) رو قبل از نمایش در باکس README به متن ساده یا Rich Text
تبدیل کن (نیازی به رندر کامل HTML نیست، فقط خوانا باشه، نه با `#`/`**` خام).

---

## در پایان:
1. برای هر مورد بالا، بعد از فیکس، توضیح بده دقیقاً چه تغییری دادی و چرا فکر می‌کنی
   این‌بار واقعاً حل میشه (مخصوصاً برای ۸، ۹، و ۱۵ که قبلاً تلاش شده و جواب نداده).
2. با `dotnet build` چک کن صفر Error/Warning جدید.
3. یه چک‌لیست نهایی از همه‌ی ۱۷ مورد (۱ تا ۱۶ + ۱۸) با یه خط توضیح برای هرکدوم بده.
