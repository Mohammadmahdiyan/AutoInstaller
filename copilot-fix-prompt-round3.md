پرامپت دور سوم — GtaSaModManager

این فایل ۱۷ بخش مستقل داره. هر بخش رو می‌تونی جدا کپی و به Copilot بدی. توی هر بخش مسیر فایل‌های مرتبط و نکات فنی دقیق نوشته شده. لطفاً قبل از هر تغییر، فایل مرتبط رو کامل بخون تا context کامل داشته باشی، و بعد از هر فیکس بگو دقیقاً چی عوض کردی و چرا.

====================================================================
بخش ۱ — لایوت سایدبار Step 3 (عکس + README) هنوز پیاده نشده
==================================================================== فایل: Forms/MainForm.cs، متد UpdateSidebarState

⚠️ تصحیح: این مورد راجع به Step 3 هست (از لحظه‌ای که کاربر مد رو انتخاب می‌کنه و برنامه تشخیصش میده)، نه Step 4. اگه قبلاً اشتباهی این تغییر روی Step 4 اعمال شده، برش‌گردون و فقط سایدبار Step 3 رو اصلاح کن.

با اینکه چندبار خواسته شده، عکس‌ها هنوز فرقی نکردن و README اصلاً نمایش داده نمیشه. این‌بار لطفاً از صفر با یه TableLayoutPanel عمودی (نه TextBox/PictureBox با سایز ثابت فعلی) داخل سایدبار Step 3 بساز:

اگه README باشه:

یه کنترل متنی read-only با word-wrap واقعی (مثل Alt+Z در VS Code — خط طولانی می‌شکنه به خط بعد، بدون اسکرول افقی؛ از TextBox با Multiline=true, WordWrap=true, ScrollBars=Vertical یا یه RichTextBox مشابه استفاده کن).
زیر این باکس، یه دکمه «Open readme.txt» که با Process.Start(new ProcessStartInfo(path) { UseShellExecute = true }) فایل رو با برنامه‌ی پیش‌فرض ویندوز باز کنه.
اگه README نبود، این کل بخش (باکس + دکمه) رندر نشه، فضاش به بخش عکس بره.

فضای باقی‌مونده (از پایین بخش README تا انتهای پنجره، یا کل فضا اگه README نبود) برای عکس‌هاست:

۰ عکس → هیچی نشون داده نشه.
۱ عکس → کل فضا (معادل جای ۴ عکس) به همین یکی اختصاص پیدا کنه.
۲ عکس → هردو زیر هم (بالا/پایین)، هرکدوم نصف فضا.
۳ عکس → دوتای اول عادی، سومی طبق جهت RTL/LTR فعلی برنامه سمت راست یا چپ چیده بشه.
۴ عکس → هر ۴ تا بدون دکمه.
بیشتر از ۴ عکس → همون ۴ تای اول + دو دکمه Next/Prev (آیکون) برای گشتن بین عکس‌ها.
تمام پدینگ‌های استاندارد پروژه (که جاهای دیگه استفاده شده) رعایت بشه.
ارتفاع کل این بخش باید دقیقاً از زیر Input انتخاب مد تا لبه‌ی پایین پنجره باشه، نه یه مقدار ثابت پیکسلی.

====================================================================
## بخش ۳ — بعد از رفت‌وآمد بین زبان‌ها، بعضی متن‌ها دوباره انگلیسی میشن
====================================================================
فایل: Services/LocalizationService.cs + Forms/MainForm.cs

سناریوی دقیق باگ: زبان رو انگلیسی می‌کنی → فارسی می‌کنی → دوباره انگلیسی می‌کنی →
بعضی متن‌ها دیگه فارسی/انگلیسی درست نمیشن. این نشونه‌ی اینه که بعضی کنترل‌ها متن‌شون
رو فقط **یک‌بار** (موقع ساخته‌شدن) از `GetString` می‌گیرن و توی تابع تغییر زبان
(`ApplyCurrentLanguage`/`ApplyLocalization`) دوباره set نمیشن — یا اینکه یه Cache/متغیر
محلی مقدار قدیمی رو نگه می‌داره و overwrite نمیشه. لطفاً:
1. کل `ApplyCurrentLanguage` رو پیدا کن و لیست کن دقیقاً کدوم کنترل‌ها توش آپدیت میشن.
2. کل MainForm.cs رو grep کن دنبال هر `.Text = _localizationService.GetString(...)`
   که **فقط داخل متد ساخت اولیه‌ی کنترل (CreateWizardStepX)** هست ولی داخل
   ApplyCurrentLanguage نیست — این‌ها دقیقاً همون کنترل‌هایی هستن که بعد از چندبار
   عوض کردن زبان "یادشون میره" آپدیت بشن. همه‌شون رو به ApplyCurrentLanguage اضافه کن.
3. مطمئن شو ApplyCurrentLanguage برای Step هایی که موقع تغییر زبان `Visible=false` ان
   هم اجرا میشه (نه فقط step فعلی)، چون وگرنه وقتی کاربر step عوض کنه با زبون قدیمی
   می‌بینه.

====================================================================
## بخش ۴ — پنجره‌ی «نصب با موفقیت انجام شد» نمی‌خوام، باید خودکار بره step بعد
====================================================================
فایل: Forms/MainForm.cs

این سه خط رو پیدا کن و هرکدوم رو حذف کن (فقط خودِ MessageBox.Show، نه منطق اطرافش):
```csharp
MessageBox.Show(_localizationService.GetString("InstallationCompleted", "Installation completed successfully."), _appName, MessageBoxButtons.OK, MessageBoxIcon.Information);
```
این خط توی سه‌جای فایل تکرار شده (نزدیک خط ۳۵۶۵، ۳۷۳۹، ۳۸۸۶ — شماره خط ممکنه بعد از
تغییرات قبلی کمی فرق کنه، با جستجوی متن دقیق پیداشون کن). بعد از حذف این پیام، مطمئن
شو که بلافاصله بعدش `GoToStep(WizardStep.Step6)` (یا مسیر مشابه) صدا زده میشه — اگه
جای این MessageBox چیزی نیست و پیشرفت کد به‌طور طبیعی ادامه پیدا نمی‌کنه، این خط رو
اضافه کن.

====================================================================
## بخش ۵ — نوار پیشرفت نصب (InstallProgressForm) باید داخل خود برنامه باشه، نه پنجره‌ی جدا
====================================================================
فایل: Forms/InstallProgressForm.cs (کلاس `InstallProgressForm : Form`)

الان این یه Form جداست که با `form.ShowDialog(this)` باز میشه (یه پنجره‌ی مجزای
ویندوزی روی برنامه میاد). می‌خوام این حذف بشه و به‌جاش یه Panel/UserControl (نه Form)
باشه که **داخل خود پنل Step4 برنامه** نمایش داده بشه (Overlay روی بخش main، دقیقاً
همون‌جایی که الان تصویر مد نشون داده میشه — طبق بخش ۶ که میگه عکس باید بره سایدبار،
این فضا آزاد میشه برای این پنل پیشرفت). مراحل:
1. `InstallProgressForm` رو به یه کلاس `InstallProgressPanel : Panel` (یا UserControl)
   تبدیل کن با همون فیلدها (ProgressBar، Label عنوان، Label وضعیت، Label فایل جاری) —
   منطق `UpdateProgress(percent, fileName)`, `Complete()`, `Fail()` رو دست‌نخورده نگه دار.
2. همه‌جای MainForm.cs که `new InstallProgressForm(...)` و `form.ShowDialog(this)` صدا
   زده میشه رو پیدا کن، و به‌جاش این پنل رو به Controls همون پنل Step4 (بخش main، جای
   قبلی عکس) اضافه کن، `BringToFront()` کن، و در پایان نصب `Visible=false`/حذفش کن.
3. مطمئن شو دیگه هیچ Form مجزایی موقع نصب باز نمیشه.

====================================================================
## بخش ۶ — بخش main در Step 4 باید نوار پیشرفت + لیست فایل‌های در حال نصب رو نشون بده
====================================================================
فایل: Forms/MainForm.cs

الان توی بخش اصلی (main) صفحه‌ی Step4 عکس مد نشون داده میشه (که باید طبق درخواست
قبلی بره توی سایدبار — اگه هنوز نرفته، این‌بار حتماً منتقلش کن)، و زیرش به‌جای لیست
فایل‌های در حال کپی، محتوای README نشون داده میشه (که اونم باید بره سایدبار). به‌جای
این‌ها، بخش main باید:
1. بالا: پنل نوار پیشرفت (همون InstallProgressPanel از بخش ۵).
2. پایین: یه لیست (ListBox یا FlowLayoutPanel از Labelها) از تمام فایل‌هایی که قراره
   نصب بشن، که وضعیت هرکدوم زنده آپدیت بشه. فرمت دقیق خواسته‌شده:
   ```
   file1.txd (1% 3% 8% 22% 40% 85% 100%)
   file1.txd
   ```
   یعنی: هر فایلی که هنوز کپی/نصبش کامل نشده، جلوی اسمش پرانتز با درصدهای پیشرفتی که
   تا الان رد شده (به‌ترتیب، جمع‌شونده — نه فقط آخرین درصد) نشون بده. وقتی به ۱۰۰٪
   رسید، پرانتز کاملاً پاک بشه و فقط اسم فایل تنها بمونه (بدون پرانتز).
   برای فایل‌هایی که هنوز نوبت‌شون نرسیده، فقط اسم فایل رو بدون پرانتز نشون بده (چون
   پیشرفتی نداشتن هنوز) — یا اگه فکر می‌کنی بهتره، به‌جاش یه رنگ خاکستری‌تر بده تا
   مشخص باشه در صف انتظاره.
3. برای اضافه کردن هر درصد جدید به پرانتز یه فایل، یه `List<int>` یا `StringBuilder`
   نگه‌دار به‌ازای هر ردیف فایل که هر بار پیشرفت جدید گزارش میشه، بهش append کن.

====================================================================
## بخش ۷ — راست‌کلیک روی عکس‌های Step 4 پنجره‌ی تمام‌صفحه باز نمی‌کنه
====================================================================
فایل: Forms/MainForm.cs

از دور قبل خواسته شده بود، هنوز کار نمی‌کنه. لطفاً این‌بار:
1. مطمئن شو تابع `OpenImageViewerFromPictureBox` (یا معادلش) واقعاً وجود داره و کار می‌کنه.
2. برای **هر PictureBoxی که توی Step4 عکس مد رو نشون میده** (چه در بخش main چه بعد از
   انتقال به سایدبار طبق بخش ۱)، مطمئن شو رویداد زیر واقعاً وصل شده:
   ```csharp
   pictureBox.MouseUp += (_, e) =>
   {
       if (e.Button == MouseButtons.Right)
       {
           OpenImageViewerFromPictureBox(pictureBox, imageListForStep4);
       }
   };
   ```
3. یه تست دستی (یا حداقل یه بررسی دقیق کد) بکن که این handler واقعاً fire میشه — ممکنه
   یه کنترل دیگه (مثلاً یه Panel شفاف روی PictureBox) جلوی MouseUp رو بگیره؛ اگه اینطوره
   یا Panel رو `Enabled=false` کن یا رویداد رو روی همون Panel بالایی وصل کن.

====================================================================
## بخش ۸ — پیام «Mod detected» توی سایدبار باید بعد از چند ثانیه ناپدید بشه (الان تا آخر می‌مونه)
====================================================================
فایل: Forms/MainForm.cs

این لیبل قبلاً به سایدبار منتقل شده (خوبه)، ولی الان **تا آخر** (تا وقتی از این step
خارج بشی) روی صفحه می‌مونه. می‌خوام برعکس بشه: بعد از چند ثانیه (مثلاً ۳-۴ ثانیه) که
نمایش داده شد، خودکار محو/مخفی بشه، بدون اینکه چیز دیگه‌ای از سایدبار (عکس/README) رو
تحت‌تأثیر قرار بده. لطفاً یه `System.Windows.Forms.Timer` با `Interval` مناسب اضافه
کن که بعد از نمایش این لیبل، یه‌بار فایر بشه و `label.Visible = false` (یا بهتر، یه
فید-اوت نرم مثل چیزی که برای RoundedButton گفتیم) کنه، و بعد خودش `Stop()`/`Dispose()`
بشه تا دوباره فایر نشه. مطمئن شو هر بار مد جدیدی تشخیص داده میشه (ورود دوباره به این
step)، لیبل دوباره نمایان بشه و تایمر از نو شروع بشه.

====================================================================
## بخش ۹ — Category در Step 5 برای بار هزارم کار نمی‌کنه
====================================================================
فایل: Forms/MainForm.cs، متد RefreshAssetStep

این مورد چندین بار قبلاً گزارش و «فیکس» شده ولی هربار برنگشته درست. این‌بار لطفاً:
1. با دقت کامل، از اول تا آخر مسیر داده رو دستی دنبال کن: از جایی که Skins.json و Vehicles.json/
   catalog خونده میشه، تا جایی که `Category` هر asset مقداردهی میشه (Services/
   AssetCatalogService.cs)، تا جایی که فیلتر `categoryFilter` ساخته و پر میشه، تا
   جایی که `_step5CategoryFilter` روی لیست asset های نمایشی اعمال میشه.
2. اگه در Skins.json و Vehicles.json مقدار Category برای این asset ها اصلاً پر نیست (خالیه یا وجود
   نداره)، این ریشه‌ی واقعی مشکله — نه کد فیلتر. در این حالت باید یا Skins.json و Vehicles.json رو
   (طبق فرمت فعلیش) اصلاح کنی که category درست داشته باشه، یا اگه کاربر می‌خواد
   دسته‌بندی خودکار از روی اسم فایل/پوشه استخراج بشه، یه قانون ساده (مثلاً بر اساس
   پیشوند اسم فایل یا یه mapping از پیش تعریف‌شده) اضافه کن.
3. هر مرحله رو با یه `Debug.WriteLine` یا لاگ موقت تست کن (تعداد category یکتا، مقدار
   انتخاب‌شده در combo، تعداد asset بعد از فیلتر) تا قبل از تحویل مطمئن بشی واقعاً کار
   می‌کنه — نه فقط کد رو عوض کنی و امیدوار باشی.

====================================================================
## بخش ۱۰ — چینش عکس‌های سایدبار Step 5 اشتباهه (باید بالا/پایین باشه نه کنار هم) + عکس مد آپدیت نمیشه
====================================================================
فایل: Forms/MainForm.cs

چیدمان درست موردنظر:
- **بالا:** عکس خودِ مد (فایلی که کاربر می‌خواد جایگزین کنه، عکس داخل پوشه‌ی مد).
- **وسط:** یه فلش رو به پایین (از عکس مد به‌سمت عکس asset انتخاب‌شده).
- **پایین:** عکس asset ای که الان از گرید انتخاب شده (اکتیوه).
- هر دو عکس باید **تمام عرض سایدبار** رو بگیرن (نه کنار هم، بالای هم، هرکدوم full-width).

**باگ مجزا:** عکس مد (بالایی) باید هر بار که وارد این step میشی یا مد عوض میشه آپدیت
بشه، ولی الان همیشه یه عکس ثابت می‌مونه (انگار فقط یه‌بار ست شده و دیگه رفرش نمیشه).
لطفاً پیدا کن کجا این PictureBox بالایی مقداردهی میشه و مطمئن شو هر بار
`PrepareDetectedAssetStep`/`GoToStep(Step5)` هم این عکس رو دوباره از مسیر واقعی مد
جاری می‌خونه، نه یه‌بار در طول عمر برنامه.

====================================================================
## بخش ۱۱ — دکمه Open Readme فقط وقتی باشه که عکس مد نباشه
====================================================================
فایل: Forms/MainForm.cs (سایدبار Step 5)

توی سایدبار Step5 (که طبق بخش ۱۰ قراره عکس مد + فلش + عکس asset باشه)، دکمه‌ی
«Open readme.txt» فقط وقتی نمایش داده بشه که مد **عکس نداشته باشه ولی README داشته
باشه**. اگه مد عکس داشت (حتی اگه README هم داشت)، این دکمه اصلاً نشون داده نشه —
چون طبق بخش ۱۰ فضای سایدبار Step5 قراره به عکس مد + فلش + عکس asset اختصاص پیدا کنه،
جایی برای README اونجا نیست.

====================================================================
## بخش ۱۲ — آیتم‌های گرید Step 5 اصلاً لود نمیشن
====================================================================
فایل: Forms/MainForm.cs، RefreshAssetStep / PrepareDetectedAssetStep

این یه باگ جدی‌تره — لطفاً اول با دقت چک کن این مشکل از کِی شروع شده (شاید یکی از
تغییرات چندتا فیکس اخیر، مثل تغییرات کارت‌سایز یا category، باعثش شده). قدم‌به‌قدم
مسیر رو دنبال کن:
1. آیا `_assetCatalogService.LoadAssets()` اصلاً asset برمی‌گردونه؟ (شاید مسیر
   Assets.json عوض شده یا فایل خراب شده باشه — چک کن `AssetCatalogService.ValidationError`
   خالیه یا نه.)
2. آیا فیلتر AssetType (Vehicle/Skin/Weapon) درست کار می‌کنه؟
3. آیا بعد از فیلتر Category (بخش ۹) که ممکنه بد پیاده بشه، لیست خالی می‌مونه؟ (یعنی
   شاید همین باگ #۱۲ عارضه‌ی مستقیم باگ #۹ باشه — دقت کن این دو تا رو با هم چک کنی
   و اگه فیکس #۹ باعث خالی موندن لیست میشه، اول همون رو درست کن.)
4. آیا کنترل‌های PictureBox واقعاً به Panel گرید `Controls.Add` میشن؟

====================================================================
## بخش ۱۳ — بعد از نصب در Step 5 به Step 6 نمی‌ره
====================================================================
فایل: Forms/MainForm.cs

این با بخش ۴ مرتبطه — اگه بعد از حذف MessageBox (بخش ۴) هنوز به Step6 نمیره، لطفاً:
1. یه try/catch با لاگ کامل (پیام خطا + StackTrace به یه فایل لاگ) دور کل مسیر نصب Step5
   بذار تا اگه Exception بی‌صدا داره جلوی GoToStep(Step6) رو می‌گیره، مشخص بشه.
2. چک کن `_selectedAssetForInstall` درست موقع کلیک Install مقدار داره (نه null شده
   باشه به‌خاطر تغییر category/columns).
3. بعد از فیکس، دقیق توضیح بده که مشکل واقعی چی بوده.

====================================================================
## بخش ۱۴ — سیستم کامل Uninstall برای مدها
====================================================================
فایل‌های مرتبط: Forms/MainForm.cs، Services/ModLoaderService.cs، Services/BackupStorageService.cs

⚠️ نکته‌ی مهم قبل از شروع: توی `Services/ModLoaderService.cs` از قبل چندتا متد نصفه‌کاره
برای uninstall هست: `TryUninstallByModId`, `TryUninstallModFolder`,
`TryUninstallUserFilesInstall`, `IsInstalled`. این‌ها **در حال حاضر هیچ‌جای MainForm.cs
صدا زده نمی‌شن** (کاملاً بی‌استفاده‌ان). اول این متدها رو بخون و بررسی کن منطق‌شون
درسته یا نه، بعد به‌جای نوشتن از صفر، همین‌ها رو (با اصلاح لازم) به UI وصل کن.

**رفتار موردنیاز، دقیقاً به این ترتیب:**

وقتی کاربر در Step 3 یه پوشه‌ی مد انتخاب می‌کنه و برنامه تشخیص میده این مد از قبل نصب
شده (با `ModLoaderService.IsInstalled` یا معادل مناسب برای هر نوع)، به‌جای پیام ساده‌ی
فعلی «آیا جایگزین شود؟»، این گزینه‌ها رو نشون بده:
1. **بله، جایگزین/نصب کن** (رفتار فعلی).
2. **[اسلحه/شخصیت/وسیله نقلیه] رو تغییر بده** — فقط وقتی نوع مد
   `vehicleandskinandweapon` یا `vehiclesandskinsandweapons` باشه. با کلیک، مستقیم
   میره Step5 (گرید انتخاب asset) و دکمه‌ی سایدبار «Next» به «Change» تغییر می‌کنه؛
   وقتی کاربر asset جدید انتخاب کرد و «Change» رو زد، فقط جایگزینی asset انجام بشه
   (uninstall قبلی + install جدید)، برگرده Step6.
3. **نه، پاک کن (Uninstall)** — طبق `NormalizedType` مد، این منطق دقیق رو اجرا کن:

   | NormalizedType | عملیات Uninstall |
   |---|---|
   | `putinmodloader` | پوشه‌ی مد داخل ModLoader (`GameService.GetModLoaderFolder`) پاک بشه. |
   | `putincleo` | پوشه‌ی مد رو بررسی کن، هر فایلی که این مد داخل CLEO ریخته پاک کن. |
   | `putingamefolder` | همون‌طور، فایل‌هایی که مستقیم داخل پوشه‌ی بازی ریخته شده پاک بشن. |
   | `putandreplace` / `putandreplaces` | فایل‌های نصب‌شده پاک بشن، و فایل‌های اصلی از بکاپ (`Services/BackupStorageService.cs`) برگردونده بشن سر جاشون، و پوشه‌ی بکاپ مخصوص همین مد (زیر پوشه‌ی ID) پاک بشه. |
   | `vehicleandskinandweapon` / `vehiclesandskinsandweapons` | همون منطق putandreplace (چون این‌ها هم فایل جایگزین می‌کنن). |
   | `savesandmissions` | همون فایل(های) سیو/DYOM که این مد نصب کرده حذف بشن (با استفاده از `TryUninstallUserFilesInstall` که از قبل هست). |
   | `missiondsl` | کل محتویات پوشه‌ی DSL که این مد ریخته پاک بشه. |

   **نکته‌ی مهم درباره‌ی بکاپ:** ساختار پوشه‌ی بکاپ باید `[ID]\[نام مد]\[فایل‌های
   اصلی همون مد]` باشه (نه همه‌ی فایل‌های اصلی همه‌ی مدها قاطی هم). یعنی هر مد یه
   ساب‌فولدر جدا زیر پوشه‌ی ID خودش داره. مطمئن شو `BackupStorageService`/
   `ModPackageService.BackupOriginalFileForReplacement` همین ساختار رو رعایت می‌کنه
   (این از قبل باید همین‌طور باشه، فقط تأیید کن).

**برای uninstall عمومی مدهای نصب‌شده (بدون نیاز به انتخاب دوباره‌ی همون پوشه‌ی مد):**
اگه برنامه یه صفحه/لیست «مدهای نصب‌شده» نداره، یکی اضافه کن (یا اگه از قبل جایی هست،
بهش وصل کن) که کاربر بتونه از یه لیست، هر مد نصب‌شده رو با دکمه‌ی آیکون سطل‌آشغال پاک
کنه — به‌خصوص برای `savesandmissions` (طبق نوشته‌ی خودت: «واقعاً نمی‌دونم چطور باید اون
سیو رو پیدا کنیم» — پیشنهاد: چون `RecordInstallation`/`TryUninstallUserFilesInstall`
از قبل فایل‌های نصب‌شده رو در یه manifest ذخیره می‌کنن، از همون manifest برای ساخت این
لیست استفاده کن، نیازی به حدس زدن نیست، رکورد دقیق موجوده).

====================================================================
## بخش ۱۵ — فیچر جدید: نصب مد اختیاری (Optional) در Step 6
====================================================================
فایل: Forms/MainForm.cs

بعد از نصب موفق یه مد، اگه پوشه‌ی مد شامل زیرپوشه‌ای باشه که اسمش (بدون توجه به بزرگی/
کوچکی حروف، یعنی lower-case) دقیقاً `optional` یا `optionals` باشه:

**حالت «optional» (یه پوشه‌ی تکی):**
- Step6 یه دکمه‌ی جدید اضافه کنه: «Install optional mod».
- موقع هاور روی این دکمه، اسم پوشه‌ی داخل optional (یعنی اسم خودِ مدِ اختیاری) به‌صورت
  تولتیپ نشون داده بشه؛ اگه این اسم خیلی طولانی بود، با `...` کوتاهش کن (یا با
  `ToolTip` استاندارد ویندوز که خودش wrap می‌کنه استفاده کن — راه‌حل خودت رو انتخاب کن،
  فقط UI به‌هم نریزه).
- با کلیک، دقیقاً مثل این باشه که کاربر از Step3 این پوشه (زیرپوشه‌ی optional) رو
  به‌عنوان مد انتخاب کرده — طبق `config.json` داخل همون پوشه نصب بشه، و تمام (لازم
  نیست خودِ optional دوباره Step6 optional بیاره).

**حالت «optionals» (چند پوشه):**
- دکمه‌ی تکی «Install optional mod» نباشه.
- به‌جاش، همون بخش عکس‌های Step6 (طبق دستورات قبلی چیدمان عکس) برداشته بشه، و **زیر
  دکمه‌های Open game folder و Run game**، یه لیست (با اسکرول اگه لازم شد) از تمام
  زیرپوشه‌های داخل optionals رندر بشه:
  - هر ردیف: چک‌باکس (تیک) + اسم مد (اسم پوشه).
  - هاور روی هر ردیف: اگه اون زیرپوشه هم عکس داشت هم README، یه پنجره باز بشه که
    عکس‌ها رو نشون بده (مطابق منطق ویوئر تمام‌صفحه‌ای که قبلاً ساختی). اگه فقط عکس
    داشت، همون؛ اگه فقط README داشت، به‌جای عکس متن README رو نشون بده؛ اگه هیچ‌کدوم
    نبود، هاور هیچ اتفاقی نندازه.
  - کلیک روی هر آیتم (نه صرفاً هاور) → دقیقاً مثل حالت «optional» بالا از Step3 نصبش
    کن، ولی این‌بار وقتی تموم شد **دوباره Step6 optionals رو نیار** — یعنی همون
    Step6 با همون لیست بمونه، فقط اون آیتمی که الان نصب شده رو با تیک مشخص کن (که
    کاربر بفهمه کدوم‌ها رو قبلاً نصب کرده) و به نصب بقیه ادامه بده.

**تایمر بسته‌شدن خودکار Step6:**
الان (طبق کد قبلی) ۶ ثانیه‌ست. اگه این مد optional/optionals داشت، این تایمر رو به
۱۵ ثانیه افزایش بده (اگه کاربر هیچ کاری نکرد، بعد از ۱۵ ثانیه ببنده؛ اگه مد optional
معمولی بدون optional/optionals بود، همون ۶ ثانیه‌ی قبلی بمونه).

====================================================================
## بخش ۱۶ — کلید جدید در config.json به اسم "require" / "requires"
====================================================================
فایل‌های مرتبط: Models/ModManifest.cs، Forms/MainForm.cs (یا یه Service جدید مثل
`Services/RequirementService.cs` که پیشنهاد می‌کنم بسازی تا منطق تمیزتر بمونه)

می‌خوایم یه سیستم "پیش‌نیاز" (requirement) اضافه بشه: بعضی مدها قبل از نصب خودشون،
لازمه یه مد دیگه (مثل CLEO یا modloader) از قبل نصب باشه؛ اگه نبود، اول اون مد پیش‌نیاز
باید نصب بشه.

**فرمت JSON (تک پیش‌نیاز، کلید `require`، یه ابجکت):**
```json
{
  "type": "putinmodloader",
  "require": {
    "checkFiles": ["cleo.asi", "modloader.asi"],
    "checkFolders": ["cleo", "modloader"],
    "reqAddress": "Scripts\\A1-MyReqFiles"
  }
}
```
**فرمت JSON (چند پیش‌نیاز، کلید `requires`، یه آرایه از همون ابجکت‌ها):**
```json
{
  "type": "putinmodloader",
  "requires": [
    { "checkFiles": ["cleo.asi"], "checkFolders": ["cleo"], "reqAddress": "Scripts\\CleoReq" },
    { "checkFile": "modloader.asi", "checkFolder": "modloader", "reqAddress": "Scripts\\ModloaderReq" }
  ]
}
```
**نکته‌ی فرمت:** اگه فقط یه فایل/پوشه برای چک کردن لازمه، به‌جای آرایه (`checkFiles`/
`checkFolders`) از فرم تکی (`checkFile`/`checkFolder`، مقدار رشته‌ای نه آرایه) استفاده
میشه. یعنی مدل باید هر دو حالت (`checkFile` رشته‌ی تکی، و `checkFiles` آرایه) رو پشتیبانی
کنه، همین‌طور `checkFolder`/`checkFolders`.

**منطق اجرا:**
1. قبل از نصب یه مد، اگه `require` یا `requires` توی config.json بود، برای هرکدوم:
   - اول تمام `checkFile(s)` رو بررسی کن: اگه **همه‌شون** توی پوشه‌ی بازی/مقصد مربوطه
     موجود بودن → این پیش‌نیاز رو **نادیده بگیر** (یعنی از قبل نصب شده، لازم نیست
     دوباره نصب بشه)، برو سراغ پیش‌نیاز بعدی (اگه چندتا بود).
   - اگه حتی یکی از `checkFile(s)` وجود نداشت → برو مرحله‌ی بعد: `checkFolder(s)` رو
     چک کن. اگه همه‌ی پوشه‌ها موجود بودن → نادیده بگیر (نصب شده حساب کن). اگه پوشه‌ها
     هم موجود نبودن → این پیش‌نیاز باید نصب بشه.
2. اگه پیش‌نیاز باید نصب بشه: از `reqAddress` (مسیر نسبی به پوشه‌ی مد اصلی —
   `Path.Combine(_selectedModPayloadPath, reqAddress)`) یه پوشه‌ی مد جدا محسوب کن و
   همون مسیر معمولی نصب مد (که برای مد اصلی استفاده میشه) رو براش صدا بزن — یعنی
   داخل اون آدرس هم باید یه config.json مستقل باشه که میگه این پیش‌نیاز چطور نصب بشه.
3. بعد از نصب موفق همه‌ی پیش‌نیازهای لازم، نصب مد اصلی رو ادامه بده.
4. اگه چندتا پیش‌نیاز (`requires`) بود، به‌ترتیب آرایه یکی‌یکی چکشون کن و لازم بود نصب کن.

====================================================================
## بخش ۱۷ — کلید جدید در config.json برای حذف فایل/پوشه‌ی ناسازگار قبل از نصب
====================================================================
فایل‌های مرتبط: Models/ModManifest.cs، Services/RequirementService.cs (یا جایی که
بخش ۱۶ رو پیاده کردی)

اسم این کلید `deleteThis` (برای یه فایل/پوشه) یا `deleteThese` (برای چندتا) هست.

**نکته‌ی طراحی مهم:** این کلید **فقط حذف می‌کنه**، جایگزینی نداره (`replaceWith` وجود
نداره). چون اگه بعد از حذف نیاز به جایگزینی باشه، `require`/`requires` (بخش ۱۶) که
طبق ترتیب زیر همیشه بعد از `deleteThis` اجرا میشه، خودش دقیقاً همون کار رو انجام
میده: چون فایل/پوشه‌ی موردنظر تازه پاک شده، `require` می‌بینه دیگه وجود نداره، پس
خودش از `reqAddress` نسخه‌ی درست رو نصب می‌کنه. نیازی به یه مکانیزم نصب جداگونه‌ی
دیگه (که کار require رو تکرار کنه) نیست.

**فرمت JSON:**
```json
{
  "deleteThis": {
    "file": "cleo.asi",
    "files": ["xxxxx.asi", "yyyyy.asi"],
    "folder": "SomeOldFolder",
    "folders": ["xxxxx", "yyyyy"]
  }
}
```
(فقط کلیدهایی که واقعاً لازمن رو بذار — همه‌شون اختیاری‌ان، هرکدوم نبود نادیده گرفته میشه.)

**مثال واقعی (مدی که با CLEO 4.4 سازگار نیست و باید CLEO 4.1 جایگزینش بشه):**
```json
{
  "deleteThis": {
    "file": "cleo.asi",
    "folder": "CLEO"
  },
  "require": {
    "checkFile": "cleo.asi",
    "checkFolder": "CLEO",
    "reqAddress": "Scripts\\CLEO 4.1"
  }
}
```
یعنی: اول `cleo.asi` و پوشه‌ی `CLEO` (نسخه‌ی ناسازگار ۴.۴، نسبت به پوشه‌ی بازی) پاک
میشن. بعد `require` اجرا میشه: چون `cleo.asi`/`CLEO` الان دیگه وجود ندارن (تازه پاک
شدن)، `require` تشخیص میده باید نصب بشن، و از `Scripts\CLEO 4.1` (نسبت به پوشه‌ی مد
اصلی/پکیج، دقیقاً مثل توضیح بخش ۱۶) نسخه‌ی درست رو نصب می‌کنه.

**منطق اجرا و ترتیب مهم:**
1. اگه هم `deleteThis`/`deleteThese` هم `require`/`requires` توی config.json مد بودن،
   اول **کامل** `deleteThis`/`deleteThese` اجرا و تموم بشه (فایل/پوشه‌های مشخص‌شده
   پاک بشن)، **بعد** برو سراغ اجرای `require`/`requires` (بخش ۱۶)، و در آخر نصب خودِ
   مد اصلی.
2. `file`/`folder` (تکی) و `files`/`folders` (آرایه) هر دو باید پشتیبانی بشن، دقیقاً
   مثل بخش ۱۶.
3. مسیر `file`/`files`/`folder`/`folders` نسبت به **پوشه‌ی بازی** حساب میشه (نه پوشه‌ی
   مد) — چون این‌ها فایل‌هایی هستن که ممکنه از یه مد/نسخه‌ی قدیمی‌تر توی بازی باقی
   مونده باشن و باید قبل از نصب این مد جدید پاک بشن.

**منطق اجرا و ترتیب مهم:**
1. اگه هم `conflictCleanup` هم `require`/`requires` توی config.json مد بودن، اول
   **کامل** `conflictCleanup` اجرا و تموم بشه (فایل/پوشه‌های مشخص‌شده پاک بشن، و اگه
   `replaceWith`/`replacesWith` بود، محتوای اون مسیر(ها) — که مثل `reqAddress` نسبت به
   پوشه‌ی مد اصلیه — جایگزین همون چیزی بشه که پاک شد)، **بعد** برو سراغ اجرای
   `require`/`requires` (بخش ۱۶)، و در آخر نصب خودِ مد اصلی.
2. `file`/`folder` (تکی) و `files`/`folders` (آرایه) هر دو باید پشتیبانی بشن، دقیقاً
   مثل بخش ۱۶.
3. مسیر `file`/`files`/`folder`/`folders` نسبت به **پوشه‌ی بازی** حساب میشه (نه پوشه‌ی
   مد) — چون این‌ها فایل‌هایی هستن که ممکنه از یه مد/نسخه‌ی قدیمی‌تر توی بازی باقی
   مونده باشن و باید قبل از نصب این مد جدید پاک/جایگزین بشن.

====================================================================
## بعد از همه‌ی بخش‌ها (وقتی همه رو دادی و انجام شد):
====================================================================
1. با `dotnet build` چک کن صفر Error/Warning جدید.
2. Models/ModManifest.cs رو بعد از بخش ۱۶ و ۱۷ کامل نشون بده تا مطمئن بشیم
   `require`/`requires`/`deleteThis`/`deleteThese` درست parse میشن (از JsonDocument خام
   استفاده کن چون ساختارشون هم می‌تونه رشته باشه هم آرایه — با `System.Text.Json` این
   یعنی باید موقع خوندن `checkFile`/`checkFiles` و `file`/`files` چک کنی `JsonValueKind`
   چیه).
3. یه چک‌لیست نهایی از هر ۱۷ بخش با یه خط توضیح چی انجام دادی بده.